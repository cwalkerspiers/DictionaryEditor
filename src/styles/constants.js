const styleConstants = {
  colors: {
    primary: "#002f67",
    grey: "slategrey",
    red: opacity => {
      return `rgba(221, 78, 65, ${opacity})`;
    }
  },
  inputAlignOffset: 0.4
};

export default styleConstants;
